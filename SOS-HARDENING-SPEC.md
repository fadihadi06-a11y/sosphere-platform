# مواصفة تقوية مسار الإنقاذ (SOS) — للمطوّر
**تاريخ:** 2026-06-10 · مبنية على تدقيق كود + حالة حيّة (مشروع `rtfhkbskgrasamhjraul`)

> ⚠️ هذه تعدّل **دالة توصيل النداءات الحيّة**. تُنفَّذ وتُختبَر عبر بيئة تطوير (Supabase CLI تتحقّق من التصريف عند النشر) + **نداء SOS تجريبي** قبل الإنتاج. لا تُنشَر عمياء.

---

## ✅ ما تم التحقّق منه (حيّ)
- مسار SOS **يعمل end-to-end**: سجلّات الدوال تُظهر نداءً حقيقياً مؤخراً: `sos-alert` 200 → `send-push-notification` 200 → `sos-alert?action=end` 200.
- إشعارات الويب (VAPID) مضبوطة (لا خطأ 503).
- `checkin_sessions` فارغ (٠ صفوف) — ميزة الرجل-الميت لم تُستخدم/تُملأ بعد.

## 🔴 اختبار يدوي عاجل (صفر مخاطر، الأهمّ الآن)
١٣ من ٢٠ جهاز إشعار نشط هي **Android** → تحتاج سرّ `FCM_SERVICE_ACCOUNT_JSON`. لم أتمكّن من قراءته عن بُعد.
**نفّذ:** أطلق SOS من حساب عامل، وتأكّد أن جهاز Android للمشرف يستلم الإشعار. إن لم يصل → `FCM_SERVICE_ACCOUNT_JSON` ناقص/خاطئ في أسرار الدوال.

---

## الإصلاحات (مرتّبة بأثر الحياة)

### P0-1 — حارس الرجل-الميت (Check-in dead-man) خادمياً
**المشكلة:** عدّاد تسجيل الحضور في العميل فقط (`checkin-timer.tsx:477,607`). لو عجز العامل وقُتل تطبيقه → لا يُطلَق SOS أبداً. لا cron على الخادم.
**النهج (آمن — يُظهر للمشرف، لا يتّصل تلقائياً):**
1. هجرة pg_cron كل دقيقة تستدعي دالة `plpgsql`.
2. الدالة تختار `checkin_sessions WHERE deadline_ts < now() - interval '2 min'` **مع dedup**: `AND NOT EXISTS (SELECT 1 FROM sos_queue q WHERE q.employee_id = cs.user_id AND q.trigger_method='missed_checkin' AND q.recorded_at >= cs.started_at)`.
3. لكلٍّ: `INSERT INTO sos_queue (id, company_id, employee_id, employee_name, zone, trigger_method, severity, status, recorded_at, type) VALUES (..., 'missed_checkin', 'high', 'active', now(), 'checkin_timeout')` → يظهر فوراً في داشبورد المشرف عبر CDC.
**شروط الأمان قبل التفعيل:**
- تأكّد من دورة حياة `checkin_sessions`: هل تُحذف الصفوف عند انتهاء الوردية بشكل طبيعي؟ إن لا، أضف شرط نشاط (`updated_at > now() - interval '12h'`) لتجنّب إطلاق وهمي لصفوف قديمة.
- اختبر **الاستعلام القرائي أولاً** (SELECT فقط) للتأكّد أنه لا يطابق صفوفاً لعمّال بخير.
- ابدأ بإشعار المشرف فقط (لا اتّصال تلقائي) — أخفّ ضرراً لو حدث إنذار كاذب.

### P0-2 — قناة بريد احتياطية للمالك في `sos-alert`
**المشكلة:** عند فشل الإشعارات (FCM ناقص / مالك بجهاز Android فقط) لا يُنبَّه أحد (`sos-alert/index.ts:2244` يرجع بصمت).
**النهج (إضافي بحت، مغلّف دفاعياً — لا يمسّ المسارات الحالية):**
- بعد كتلة إشعار المالك (بعد سطر ~2216)، أضف داخل `try/catch` منفصل: حلّ بريد المالكين، ثم أرسل عبر **Resend API** (نفس نمط `send-invitations/index.ts:186-193`)، من `noreply@sosphere.co` (النطاق موثّق الآن).
- بريد المالكين: استعلم من `auth.users` عبر service-role، أو من جدول profiles/employees.
- استخدم `RESEND_API_KEY` (موجود أصلاً لـ send-invitations، والأسرار مشتركة).
- **حرج:** لفّ كل الكتلة في `try/catch` لا يرمي أبداً — البريد قناة احتياطية، فشلها يجب ألا يؤثّر على push/SMS/realtime.

### P1-3 — تنبيه المشرف قبل Twilio
**المشكلة:** إشعار المالك (`sos-alert:2216`) والبثّ (`2432`) يأتيان **بعد** `await Promise.all` على fan-out الاتصالات (`1790`) → تأخير حتى ~٢٠ ثانية في تنبيه المشرف حين Twilio بطيء/معطّل.
**النهج:** انقل إشعار المالك + بثّ realtime ليُنفَّذا **قبل** أو **بالتوازي** مع fan-out الاتصالات (عبر `backgroundOrAwait` الموجود). المشرف يُنبَّه أولاً، ثم تجري محاولات الاتصال.

### P1-4 — بثّ `sos-live` الميّت
**المشكلة:** `sos-alert:2432` يبثّ على قناة لا يشترك فيها أي مشرف (الداشبورد يعتمد على CDC لـ `sos_queue` فقط).
**النهج:** إمّا اشترك الداشبورد في `sos-live:${companyId}`، أو احذف البثّ واعتمد CDC (وثّق المصدر الوحيد).

### P1-5 — لا تُبالغ في تأكيد التوصيل (✅ أُصلح ونُشر)
- `sos-emergency.tsx`: لا نقول "أُرسل" حين لا يصل أحد فعلاً (تحقّق من SIDs).
- `offline-sync.tsx`: نصّ صادق + "اتّصل بالطوارئ مباشرةً".

### P2-6 — حارس prewarm
`sos-server-trigger.ts:780` يزعم وجود cron يصعّد prewarm — **غير موجود**. ابنِه أو احذف الزعم.

---

## ملخّص الأولوية
1. **الآن (يدوي):** اختبار SOS حيّ لتأكيد FCM/Android.
2. **P0:** حارس الرجل-الميت + قناة بريد المالك.
3. **P1:** ترتيب الإشعار + البثّ الميّت.
كلها تُنشَر مع **نداء SOS تجريبي** للتأكيد.
