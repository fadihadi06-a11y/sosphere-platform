# تقرير فحص منصة SOSphere — الترابط الكامل (خلية النحل)
**التاريخ:** 2026-06-09 · **المشروع:** sosphere-platform · **قاعدة البيانات:** Supabase `rtfhkbskgrasamhjraul` (نشطة وسليمة)

---

## 0) الخلاصة التنفيذية

البنية تتكوّن من **ثلاث طبقات + خلفية واحدة**، وهي مترابطة فعلاً «كخلية نحل» في معظمها، مع **ثغرات محدّدة قابلة للإصلاح** — لا يوجد خلل معماري جذري.

| الطبقة | الحالة | الحكم |
|---|---|---|
| المنصة (الويب التعريفي + القانوني) | المسارات سليمة | ✅ يعمل |
| التطبيق المحمول `/app` | 32 شاشة، تنقّل متماسك | ✅ يعمل مع نواقص |
| لوحة المالك/الأدمن `/dashboard` | ~28 صفحة خلف حارس + PIN + MFA | ⚠️ يعمل مع أزرار وهمية |
| الخلفية (Supabase) | 140+ جدول كلها RLS مفعّل | ✅ مترابطة وموثّقة |

**أهم 3 أمور تحتاج قراراً:**
1. مفهوم «المالك / السوبر أدمن» في الواجهة **غير مربوط بالكامل** بعمود `owner_id` في قاعدة البيانات (تعريف جزئي وهمي).
2. صفحة **Governance** مخفية من القائمة لكن **بلا بوابة عند العرض** → يمكن الوصول إليها برابط مباشر (deep-link).
3. ~12 زراً «شكلياً» (toast فقط أو "قريباً") في الإعدادات والفوترة و SAR.

> ملاحظة منهجية: تعذّر تشغيل البناء/الاختبارات (`build`/`test`/`lint`) لأن بيئة Linux المعزولة لم تُقلع في هذه الجلسة. الفحص تمّ بقراءة الشيفرة مباشرة + فحص قاعدة البيانات الحيّة.

---

## 1) المسارات (Routing) — هل كلها صحيحة؟ ✅

ملف التوجيه `src/app/routes.ts` نظيف ومتماسك:

- `/` صفحة الهبوط (وعلى التطبيق الأصلي يُعاد التوجيه تلقائياً إلى `/app`).
- `/app` التطبيق المحمول · `/dashboard` لوحة التحكم (خلف حارس) · `/welcome` · `/demo` · `/training` · `/compliance`.
- صفحات قانونية: `/privacy` · `/terms` · `/legal/dpa`.
- معالجات الروابط العميقة: `/auth/callback` · `/reset-password` · `/payment-success` · `/payment-cancelled` · `/shared-sos/:emergencyId`.
- `*` صفحة 404.
- كل الصفحات الثقيلة محمّلة كسولاً (lazy) مع شاشة تحميل — أداء جيد.

**الحكم:** المسارات العالية المستوى صحيحة ومكتملة. لا روابط ميتة على مستوى الراوتر.

---

## 2) التطبيق المحمول `/app` — التنقّل والأزرار

**النموذج:** آلة حالات (state machine) من 32 شاشة، مع تاريخ تنقّل وزر رجوع للأندرويد، وبوابة MFA قبل الشاشات النهائية (وتُتجاوز عند SOS — الأولوية للحياة). ✅

**مسارات الأدوار:**
- مدني/فرد: welcome → role-select → login-phone → الموافقات → individual-register → individual-home.
- موظف شركة: … → company-join → (إعداد سريع أو بانتظار الموافقة) → employee-dashboard.
- مسار سريع للموظف صاحب عضوية فعّالة، وتسجيل دخول Google مع استعادة حالة من الخادم.

**تدفّق SOS (القلب النابض):** مكتمل من الضغط → استدعاء خادم `sos-alert` (Twilio + إشعار الجيران + watchdog) → الاتصال المحلي بالجهات → التسجيل → الحفظ → التقرير → الاستعادة بعد إعادة التشغيل (تحقّق من جدول `sos_sessions`). ✅ لا فجوات.

**النواقص:**
- 🔴 زرّا **"دائرة العائلة" (Family Circle)** و **"الموقع المباشر" (Live Location)** في الصفحة الرئيسية للفرد يظهران لكن **بلا معالج** — لا توجد الشاشتان أصلاً في التطبيق. النقر لا يفعل شيئاً بصمت (`individual-home.tsx` سطر ~434، والمعالجات غير مُمرّرة في `mobile-app.tsx`).
- 🟡 قيمة الشاشة `"onboarding"` في الـ enum **غير قابلة للوصول** (شيفرة ميتة).
- 🟡 شاشة الاستعادة الاحتياطية بعد SOS تجربتها محرجة (عدّاد 60ث بلا زر خروج واضح).

---

## 3) لوحة المالك/الأدمن `/dashboard` — التنقّل والأزرار

**النموذج:** تنقّل عبر «محاور» (Hubs) منظّمة بأولوية الخطر: الطوارئ → الذكاء الأمني → العمليات → الأشخاص → الحوادث/المخاطر → التقارير → الحوكمة. القائمة الجانبية تُفلتر العناصر حسب الدور. ✅ متماسك.

**~28 صفحة موصولة**، و**5 ملفات «يتيمة»** (لوحات/نوافذ مساعدة لا صفحات كاملة) — ليست خللاً.

**النواقص (أزرار شكلية / غير موصولة):**
- 🟡 **الإعدادات:** رفع الشعار، تدوير مفتاح API، تصدير سجل التدقيق، باني الجداول — كلها **toast فقط** بلا تنفيذ.
- 🟡 **الفوترة:** الإضافات (add-ons) معطّلة "قريباً"، ونموذج الدفع غير مكتمل (مؤجّل لـ Stripe v1.1).
- 🟡 **SAR:** الإرسال المباشر معطّل "قريباً".
- 🟡 **الموظفون:** زرّا الاتصال/الرسالة يُظهران toast دون اتصال فعلي.
- 🔴 **Governance:** القائمة تُفلترها بالدور، لكن **لا بوابة عند العرض** → الوصول ممكن برابط مباشر (تجاوز).

**ربط الخلفية:** هجين —
- ✅ حيّ: الطوارئ، الموظفون، المناطق (Zustand + Supabase realtime).
- 🟡 مؤجّل (تعليقات `SUPABASE_MIGRATION_POINT`): الإعدادات، لوحة المتصدّرين، سجل التدقيق — لم تُربط بعد بالبيانات الحيّة.
- 🟡 بيانات تجريبية/احتياطية: نظرة عامة (الخط الزمني/الصحة)، تحقيق الحوادث (وضع DEV).

---

## 4) السوبر أدمن ↔ المنصة ↔ اللوحة ↔ قاعدة البيانات

**الحارس (`utils/dashboard-auth-guard`):** الوصول إلى `/dashboard` **متحقَّق من الخادم** عبر RPC `getAuthenticatedRole()` + JWT من Supabase، ثم PIN + MFA داخل الصفحة. التخزين المحلي للعرض فقط ولا يُعتمد عليه للمصادقة. ✅ جيد.

**الإجراءات الحسّاسة** تمرّ عبر **~10 دوال خادمية (Edge Functions)** مع RPCs `verify_permission` و `verify_sensitive_op` التي تفرض الصلاحية على الخادم. ✅ جيد.

**نموذج الأدوار:** 8 أدوار بمصفوفة صلاحيات متدرّجة (27 صلاحية للأعلى نزولاً إلى 3 للموظف).

### ماذا يستطيع المالك أن يدير؟ ✅
الفوترة والاشتراك (ترقية/إلغاء التجربة/الدفع/قبول DPA) · إدارة الفريق (الأدوار، الصلاحيات، مديرو المناطق، الدعوات) · الإعدادات والمفاتيح · إرسال الطوارئ، الموظفون، المناطق والـ Geofencing، البثّ، التقارير.

### ماذا لا يستطيع المالك أن يديره من الواجهة؟ (يتطلّب لوحة Supabase / SQL / متغيّرات بيئة)
أعلام الميزات (feature_flags فارغ ومدفوع بالبيئة) · إعداد الأسعار/الخطط (يُدار عبر Stripe) · تعليق/حذف شركة · سياسات RLS · أسرار دوال الخادم · مدة حفظ سجل التدقيق · إعداد البريد/SMTP · تعدّد الشركات · التصدير الجماعي للبيانات.

### مخاطر/فجوات في الصلاحيات
- 🔴 **عدم تطابق «المالك»:** القاعدة فيها `companies.owner_id` لكن مستوى «owner» في الواجهة جزئياً **وهمي وغير مربوط** بالقاعدة.
- 🔴 **صلاحيات مخصّصة غير مفروضة بـ RLS** (تحقّق في الواجهة فقط) → قابلة للتجاوز نظرياً.
- 🟡 تحديثات تفاؤلية قبل تأكيد الخادم · لا تحقّق بريد على الدعوات · بعض فحوص الجلسة قديمة.

---

## 5) فحص قاعدة البيانات الحيّة (Supabase Advisors)

**الأمان:** **0 أخطاء (ERROR)** · 22 INFO · 200+ تحذير (WARN). **كل الجداول (140+) مفعّل عليها RLS** ✅. التحذيرات يغلب عليها:
- دوال `SECURITY DEFINER` قابلة للتنفيذ من دور `anon` (يُنصح بمراجعة منح EXECUTE).
- جداول «خدمة فقط» عليها RLS بلا سياسة (deny-all مقصود — سليم).
- امتداد `pg_net` في schema العام.

**الأداء:** ~68 مفتاحاً أجنبياً بلا فهرس · ~120+ فهرساً غير مستخدم · ~180+ سياسة سماح متعدّدة (إعادة تقييم) · جدول واحد بلا مفتاح أساسي. كلها **تحسينات** لا معطّلات.

---

## 6) خطوات عالمية المستوى — خارطة طريق مرتّبة بالأولوية

### P0 — قبل أي إطلاق (أمان/سلامة)
1. **اربط «المالك» بالقاعدة:** اجعل مستوى owner في الواجهة يقرأ `companies.owner_id` فعلياً، وافرضه في RLS، لا في الواجهة فقط.
2. **بوابة عرض لصفحة Governance** (وأي صفحة أدمن) — لا تكتفِ بإخفائها من القائمة؛ امنع العرض عند الوصول المباشر.
3. **افرض الصلاحيات المخصّصة على مستوى RLS** بحيث لا يمكن تجاوزها من العميل.
4. **راجع منح EXECUTE لدور anon** على دوال SECURITY DEFINER، واقصرها على `authenticated` حيثما أمكن.

### P1 — اكتمال الوظائف (تجربة المستخدم)
5. **التطبيق:** إمّا تنفيذ شاشتي Family Circle و Live Location، أو إخفاء زرّيهما حتى الجاهزية.
6. **اللوحة:** وصّل الأزرار الشكلية (رفع الشعار، تدوير API، تصدير التدقيق، الاتصال/الرسالة) أو أخفِها مؤقتاً مع وسم واضح "قيد التطوير".
7. أكمل ربط الصفحات المؤجّلة (`SUPABASE_MIGRATION_POINT`): الإعدادات، المتصدّرون، سجل التدقيق — واستبدل أي بيانات تجريبية ببيانات حيّة.
8. أضف **تحقّق البريد على الدعوات**، واستبدل التحديثات التفاؤلية بتأكيد من الخادم في الإجراءات الحسّاسة.

### P2 — تحسينات وتنظيف
9. أضف فهارس للمفاتيح الأجنبية كثيرة الاستعلام، واحذف الفهارس غير المستخدمة، ووحّد سياسات السماح المتعدّدة.
10. أزل الشيفرة الميتة (شاشة `onboarding`، الملفات اليتيمة)، وحسّن تجربة شاشة استعادة SOS.
11. وفّر للمالك تحكّماً واجهياً بما يُدار اليوم يدوياً (أعلام الميزات، مدة حفظ التدقيق، التصدير الجماعي) إن كان ضمن نطاق منتجك.

---

## 7) الحكم النهائي
المنصة والتطبيق واللوحة **مترابطة ومتناغمة فعلاً** على مستوى المسارات والمصادقة الخادمية وقاعدة بيانات محكمة بـ RLS. «خلية النحل» قائمة — لكنها تحتاج **شدّ ثلاثة براغٍ أمنية (P0)** وإكمال **أزرار وشاشات ناقصة (P1)** قبل وصفها بـ«جاهزة عالمياً». لا توجد أعطال جذرية.
