# SOSphere — قائمة جاهزية الإطلاق
**تاريخ:** 2026-06-10 · النطاق: `https://sosphere.co`

---

## 📸 لقطة الحالة الآن

المنصّة **حيّة وتعمل** على `sosphere.co`: صفحة اللاندنك تظهر، الدخول الكامل (OTP→MFA→PIN) يعمل، البريد يصل من نطاقك. بقيت بنود تحقّق/إكمال قبل إطلاق كامل الثقة.

---

## ✅ أُنجز في هذه الجلسة

| البند | الحالة |
|---|---|
| ظهور صفحة اللاندنك على الويب (كانت تُحوّل للموبايل) | ✅ منشور |
| اتجاه النص يتبع اللغة (لا أرقام/ترقيم مقلوب) | ✅ منشور |
| مؤشّر تحميل بدل الشاشة السوداء | ✅ منشور |
| النطاق `sosphere.co` + `www` يعملان | ✅ |
| المسارات المتداخلة (`/legal/dpa`, `/auth/callback`, `/shared-sos`) | ✅ منشور (كانت معطّلة كلياً) |
| البريد عبر Resend من `noreply@sosphere.co` (لأي مستخدم) | ✅ |
| طول OTP = 6 يطابق الواجهة | ✅ |
| تدفّق الدخول الكامل مُختبَر end-to-end | ✅ |
| ضجيج CSP/eval في Sentry (دون إضعاف الأمان) | ✅ منشور |
| CodeQL CI لا يُعطّل الـ pipeline | ✅ منشور |

---

## 🔴 P0 — تحقّق/أصلح قبل الإطلاق الحقيقي

### 1. متغيّرات بيئة Vercel الحرجة (تحقّق أنها مضبوطة)
هذه تُبنى في العميل؛ غيابها يُعطّل ميزات حياتية صامتاً:
- [ ] **`VITE_SENTRY_DSN`** — بدونه أعطال الإنتاج **غير مرئية** (الكود يصفه: "نظام إنقاذ حياة — يجب ضبط Sentry").
- [ ] **`VITE_FIREBASE_API_KEY`** — بدونه **إشعارات الدفع معطّلة**، فالعمّال لا يستلمون تنبيهات SOS في الخلفية.
- [ ] **`VITE_TWILIO_ENABLED=true`** (عند إكمال Twilio).
- [ ] **`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`** — (تعمل، الدخول نجح ✅).

### 2. أسرار Supabase Edge Functions — تحقّق من قيمتين
- [ ] **`SOSPHERE_BASE_URL`** = `https://sosphere.co` (قالب المشروع فيه الرابط القديم `…vercel.app` — صحّحه إن نُسخ منه، وإلا ردود Twilio/الروابط تذهب للنطاق الخطأ).
- [ ] **`ALLOWED_ORIGINS`** للدوال يجب أن يتضمّن `https://sosphere.co` (القيمة الافتراضية في الكود تذكر vercel.app فقط — تحقّق أنه مضبوط كسرّ يشمل نطاقك).

### 3. Twilio — المكالمات الهاتفية الحقيقية (PSTN)
- [ ] اجتياز **توثيق هوية Twilio** (معطّل خارجياً حالياً).
- [ ] إنشاء **API Key** + **TwiML App** وإضافة الأسرار: `TWILIO_API_KEY_SID`, `TWILIO_API_KEY_SECRET`, `TWILIO_TWIML_APP_SID`, `SOSPHERE_DEFAULT_COUNTRY`.
- [ ] الموجود: `ACCOUNT_SID`, `AUTH_TOKEN`, `FROM_NUMBER` (يكفي للمكالمات الخادمية).
- ملاحظة: حتى يكتمل، المكالمات تتراجع لـ WebRTC (متصفّح-لمتصفّح، بلا اتصال هاتفي). **ليس حاجز إطلاق صلب** لكنه أساسي لمنصّة طوارئ.

---

## 🟡 P1 — مهمّ بعد الإطلاق مباشرةً

- [ ] **تمشية حيّة لتدفّق المالك** — التحقّق البصري من معالج إنشاء الشركة والداشبورد (لم يُختبر بصرياً بعد).
- [ ] **التحقّق أن "ضجيج eval" غير ضارّ** — جرّب تصدير PDF (صفحة DPA) والرسوم البيانية بعد الدخول؛ إن عملا فالفلترة صحيحة.
- [ ] **فجوتا UX مرشّحتان** (من الكود، تُؤكَّد حيّاً):
  - بريد العمل **"اختياري"** عند إنشاء الشركة — لمنصّة B2B يُفضّل جعله موثّقاً.
  - **لا توجد قائمة "خطوات أولى"** بعد التسجيل — يُنصح بإضافة setup checklist (ادعُ فريقك، أنشئ منطقة، فعّل الفوترة).
- [ ] **محتوى `/terms`** قصير (٩٣٨ حرفاً) — تأكّد أنه كامل قانونياً.

---

## 🟢 P2 — تحسينات/تلميع

- [ ] تأخير شاشة البوت ١.٥ ثانية في `index.html` — يمكن تقليله لأداء أسرع.
- [ ] السرّ الزائد `TWILIO_PHONE_NUMBER` في Supabase (الكود يستخدم `FROM_NUMBER`) — غير ضارّ، يمكن حذفه.
- [ ] **CodeQL / GitHub Advanced Security** — الفحص الأمني الكامل مدفوع (~$49/شهر للمستودع الخاص)؛ حالياً غير مُعطِّل. قرار اختياري.
- [ ] توثيق نطاق Resend مكتمل ✅ (بند مُنجز).

---

## 📞 بدائل Twilio (قرار ما بعد الإطلاق)

الكود مكتوب لـ Twilio؛ أي بديل يتطلّب **إعادة كتابة طبقة الصوت**:
- **Unifonic** — سعودي (الرياض)، الأنسب للسوق المحلي.
- **Sinch / MessageBird** — أقوياء في الشرق الأوسط.
- **Vonage / Plivo / Telnyx** — منافسون، أرخص أحياناً.

**التوصية:** أكمل Twilio (عقبة توثيق لمرة واحدة)؛ تغيير المزوّد لاحقاً فقط إن لزم.

---

## ▶️ الترتيب المقترح للعمل

1. تحقّق من متغيّرات Vercel (Sentry + Firebase) — **الأهم، وسريع**.
2. تحقّق من `SOSPHERE_BASE_URL` و `ALLOWED_ORIGINS` في Supabase.
3. تمشية حيّة لتدفّق المالك + إصلاح فجوات UX (نشر واحد مجمّع).
4. Twilio حين ينجح التوثيق.
